services:
  app:
    build:
      target: production
    restart: unless-stopped
    environment:
      NODE_ENV: production
      LOG_LEVEL: info
    ports:
      - "3000:3000"
    command: ["node", "dist/main.js"]

  postgres:
    restart: unless-stopped

  rabbitmq:
    restart: unless-stopped
